[package]
name = "corelib-probes"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
corelib-rust = { path = "../../rust" }
tokio = { version = "1.52.3", features = ["full"] }

[target.'cfg(loom)'.dependencies]
loom = "0.7"

[features]
loom = []
