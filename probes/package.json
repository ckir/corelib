{
    "name": "@ckir/corelib-probes",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "probe": "vitest run --config vitest.config.ts"
    },
    "devDependencies": {
        "@types/node": "^25.9.2",
        "vitest": "^4.1.8"
    }
}
