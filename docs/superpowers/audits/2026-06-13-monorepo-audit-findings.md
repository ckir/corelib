# Monorepo Audit — Ranked Findings Backlog

> Generated: 2026-06-13 · Source: `.agent/audit_scratchpad.json` · Total findings: **28** · Clusters: **15**
> Do NOT edit this file by hand — regenerate via `node probes/tools/synthesize.mjs`.

## Summary

### By severity

| Severity | Count |
|----------|-------|
| critical | 0 |
| high | 3 |
| medium | 13 |
| low | 12 |

### By confidence

| Confidence | Count |
|------------|-------|
| confirmed-by-probe | 4 |
| confirmed-by-reading | 23 |
| suspected | 1 |

### By zone

| Zone | Count |
|------|-------|
| phase0 | 9 |
| engine | 4 |
| ffi | 2 |
| boot | 9 |
| facade | 4 |

---

## Ranked Clusters

> Ownership rule: within a cluster, the finding from the **lowest boundary zone** is the OWNER (zone order: `phase0 < engine < ffi < boot < facade`). Higher-zone members are listed as manifestations/affected-surfaces of the root cause.

### Cluster: `configmanager-cli-argv-hazards`

**Max severity:** high · **Best confidence:** confirmed-by-probe · **Members:** 2

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `boot-ConfigManager-cli-override-process-exit-07` |
| zone | boot |
| severity | high |
| confidence | confirmed-by-probe |
| lenses | edge, arch |
| os_sensitivity | cross-os |
| testability | A |
| probe | `probes/js/configmanager-init-race.probe.test.ts` |
| evidence | ts-core/src/configs/ConfigManager.ts:168-172 —  initialize() builds a commander Command with allowUnknownOption(true) (:170) intending unknown --flags to flow to applyCliOverrides() (:391-418, the documented step-5 CLI-override mechanism). But under commander@15, parseAsync (:172) classifies an u... |
| fix_sketch | Stop commander from exiting the process inside a library: call program.exitOverride() (throw instead of exit) and catch, and/or program.allowExcessArguments(true) so unknown tokens are retained in ... |

#### Manifestations / affected surfaces

| id | zone | severity | confidence | lenses | os_sensitivity | testability | evidence (snippet) |
|----|------|----------|------------|--------|----------------|-------------|-------------------|
| `boot-ConfigManager-process-argv-unguarded-08` | boot | medium | confirmed-by-reading | edge,arch | linux-only | A | ts-core/src/configs/ConfigManager.ts:165 —  initialize() does `const argv = args ?? process.argv.slice(2)`; process.argv is accessed unguarded. On edge runtimes where process is a partial shim with... |

---

### Cluster: `configmanager-concurrency-races`

**Max severity:** high · **Best confidence:** confirmed-by-reading · **Members:** 4

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `boot-ConfigManager-initialize-races-01` |
| zone | boot |
| severity | high |
| confidence | confirmed-by-reading |
| lenses | races, arch |
| os_sensitivity | cross-os |
| testability | B |
| probe | — |
| evidence | ts-core/src/configs/ConfigManager.ts:160-190 —  async initialize() has NO double-init/concurrent-init guard. It is async with multiple await points (parseAsync :172, fetchExternalConfig :177, applyCliOverrides :185). Two concurrent callers (e.g. Promise.all([cm.initialize(argsA), cm.initialize(ar... |
| fix_sketch | Add an init guard: cache a private _initPromise on first initialize() call and return it for subsequent concurrent calls (idempotent single-flight), or throw on second init. If re-init with differe... |

#### Manifestations / affected surfaces

| id | zone | severity | confidence | lenses | os_sensitivity | testability | evidence (snippet) |
|----|------|----------|------------|--------|----------------|-------------|-------------------|
| `boot-ConfigManager-partial-init-window-02` | boot | high | confirmed-by-reading | races,edge | cross-os | B | ts-core/src/configs/ConfigManager.ts:115-120,160-190 —  getInstance() returns a usable singleton whose _config starts as {} (:87) and is only populated after the async initialize() resolves. Any re... |
| `boot-ConfigManager-loadExternalConfig-concurrent-05` | boot | medium | confirmed-by-reading | races | cross-os | B | ts-core/src/configs/ConfigManager.ts:204-230 —  loadExternalConfig() is a public async runtime mutator with no guard against concurrent invocation or concurrency with initialize(). It awaits fetchE... |
| `boot-ConfigManager-sysconfig-reference-severance-09` | boot | medium | confirmed-by-reading | races,edge | cross-os | B | ts-core/src/configs/ConfigManager.ts:107-109 (constructor binds globalThis.sysconfig = this._config, the EMPTY initial object = Ref A) vs :162 (loadDefaults reassigns this._config = Ref B) and :363... |

---

### Cluster: `redb-double-open-process-abort`

**Max severity:** medium · **Best confidence:** confirmed-by-probe · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `engine-redb-open-expect-abort-01` |
| zone | engine |
| severity | medium |
| confidence | confirmed-by-probe |
| lenses | races, edge |
| os_sensitivity | cross-os (Linux leg pending-ci; redb file-locking differs Win/Linux) |
| testability | C |
| probe | `probes/rust/tests/redb_concurrent.rs::q3_shared_path_double_open_panics` |
| evidence | rust/src/markets/nasdaq/datafeeds/streaming/core/host.rs:57 —  WebsocketStreamerHost::new does `Database::create(&db_path).expect("Failed to open redb")`. A failed redb open (path already open in-process => redb DatabaseAlreadyOpen, locked file, unwritable/missing dir, permission) PANICS, which a... |
| fix_sketch | Replace .expect() with `?`-propagation: have WebsocketStreamerHost::new return napi::Result<Self> (or a Result the napi layer maps to a JS Error) so a redb open failure surfaces as a catchable JS e... |

---

### Cluster: `http-retry-config-hazards`

**Max severity:** medium · **Best confidence:** confirmed-by-reading · **Members:** 2

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `boot-RequestUnlimited-retry-limit-unbounded-03` |
| zone | boot |
| severity | medium |
| confidence | confirmed-by-reading |
| lenses | edge, perf |
| os_sensitivity | cross-os |
| testability | B |
| probe | — |
| evidence | ts-core/src/retrieve/RequestUnlimited.ts:112-116,121-130 —  cfgRetryLimit is read from ConfigManager.get(retrieve.retry.limit) with NO upper-bound validation and fed straight into ky retry.limit. A malformed/poisoned config value (e.g. retrieve.retry.limit = 100000, or a non-number coerced throug... |
| fix_sketch | Clamp/validate config-derived retry params: coerce to integer, enforce a sane MAX (e.g. limit <= 10, backoffLimit <= 30000) before passing to ky; reject/log out-of-range values and fall back to the... |

#### Manifestations / affected surfaces

| id | zone | severity | confidence | lenses | os_sensitivity | testability | evidence (snippet) |
|----|------|----------|------------|--------|----------------|-------------|-------------------|
| `boot-RequestUnlimited-backoff-no-jitter-04` | boot | low | confirmed-by-reading | perf,races | cross-os | C | ts-core/src/retrieve/RequestUnlimited.ts:41-54 —  retry config sets limit/backoffLimit but provides no jitter; ky default backoff is deterministic exponential (0.3*2^n capped at backoffLimit 3000ms... |

---

### Cluster: `error-serialization-log-gaps`

**Max severity:** medium · **Best confidence:** confirmed-by-reading · **Members:** 4

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `phase0-logger-raw-error-sqlite-01` |
| zone | phase0 |
| severity | medium |
| confidence | confirmed-by-reading |
| lenses | arch |
| os_sensitivity | cross-os |
| testability | A |
| probe | — |
| evidence | ts-core/src/database/sqlite/sqlite-db.ts:49,114 —  raw caught error `e` passed directly as `error: e` into logger extras without serializeError(). Non-Error objects or Error subclasses with non-enumerable properties will serialize as `{}` in JSON structured logs, losing the error message, stack, ... |
| fix_sketch | Wrap both occurrences with serializeError: `error: serializeError(e)`. serializeError is already used in ts-core (see core/index.ts:9). NOT implemented this cycle. |

#### Manifestations / affected surfaces

| id | zone | severity | confidence | lenses | os_sensitivity | testability | evidence (snippet) |
|----|------|----------|------------|--------|----------------|-------------|-------------------|
| `phase0-logger-raw-error-postgres-01` | phase0 | medium | confirmed-by-reading | arch | cross-os | A | ts-core/src/database/postgres/postgres-db.ts:50,115 —  raw caught error `e` passed directly as `error: e` into logger extras without serializeError(). Same issue as sqlite-db: non-enumerable Error ... |
| `phase0-logger-raw-error-router-01` | phase0 | medium | confirmed-by-reading | arch | cross-os | A | ts-cloud/src/core/router.ts:137 —  `error: err.message` in onError handler only captures message string, discarding stack trace and cause chain. In production this leaves unhandled router errors lo... |
| `phase0-logger-raw-error-Top100-01` | phase0 | medium | confirmed-by-reading | arch | cross-os | A | ts-markets/src/nasdaq/groups/Top100.ts:125 —  `error: error instanceof Error ? error.message : String(error)` manually extracts message string; discards stack, cause, and non-enumerable fields. Usi... |

---

### Cluster: `worker-bundle-size-and-platform`

**Max severity:** medium · **Best confidence:** confirmed-by-reading · **Members:** 2

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `facade-worker-bundle-size-perf-01` |
| zone | facade |
| severity | medium |
| confidence | confirmed-by-reading |
| lenses | perf, edge |
| os_sensitivity | linux-only |
| testability | C |
| probe | — |
| evidence | ts-cloud/src/platform/cloudflare/worker.ts —  wrangler deploy --dry-run reports Total Upload: 6438.69 KiB / gzip: 901.18 KiB for a single-file worker bundle. Oversized contributors confirmed by bundle inspection: (1) ts-core/src/loggers/implementations/gcp.ts imports @google-cloud/pino-logging-gc... |
| fix_sketch | Change tsup cloudflare worker entry to platform:"browser" so Node built-ins are excluded and bundle size drops significantly. Replace noExternal:[/.*/] with a targeted allowlist of deps that are ac... |

#### Manifestations / affected surfaces

| id | zone | severity | confidence | lenses | os_sensitivity | testability | evidence (snippet) |
|----|------|----------|------------|--------|----------------|-------------|-------------------|
| `facade-worker-tsup-platform-node-01` | facade | medium | confirmed-by-reading | edge,arch | linux-only | A | ts-cloud/tsup.config.ts:8 —  the cloudflare worker bundle entry uses platform:"node" (esbuild node platform). This tells esbuild that Node.js built-in modules are available, so it does NOT mark nod... |

---

### Cluster: `market-status-http-error-swallow`

**Max severity:** medium · **Best confidence:** confirmed-by-reading · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `facade-market-status-error-status-swallowed-01` |
| zone | facade |
| severity | medium |
| confidence | confirmed-by-reading |
| lenses | arch |
| os_sensitivity | cross-os |
| testability | A |
| probe | — |
| evidence | ts-cloud/src/markets/nasdaq/MarketStatusCloud.ts:42,54 —  the catch block constructs a fatalResult object and returns c.json(fatalResult, 200) hardcoded. This swallows the HTTP error status: a caller cannot distinguish a real 200 success from a fatal internal error by HTTP status alone; both retu... |
| fix_sketch | Change the catch-branch return from c.json(fatalResult, 200) to c.json(fatalResult, 500) (or read the status from the thrown error if it carries one). This aligns with all other routers in the file... |

---

### Cluster: `ffi-reentrancy-reconnect-gc`

**Max severity:** medium · **Best confidence:** suspected · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `ffi-reentrancy-reconnect-gc-deadlock-01` |
| zone | ffi |
| severity | medium |
| confidence | suspected |
| lenses | races |
| os_sensitivity | cross-os |
| testability | A |
| probe | — |
| evidence | HYPOTHESIS (spec §8): under rapid subscribe/unsubscribe churn + forced reconnects + JS GC, the Rust->JS threadsafe_function (on_pricing/on_market, fired NonBlocking from the alpaca pump closure alpaca_streamer.rs:184-221) could DEADLOCK/STARVE the JS event loop or fire AFTER teardown (Drop, host.... |
| fix_sketch | No code change. To raise confidence to confirmed-suspected-clean or flip to a real finding, the probe must actually drive DELIVERED>0: (1) in the loopback, defer/repeat streamTrade frames AFTER the... |

---

### Cluster: `ts-core-node-imports-edge-compat`

**Max severity:** low · **Best confidence:** confirmed-by-probe · **Members:** 5

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `phase0-ts-core-node-module-SysInfo-01` |
| zone | phase0 |
| severity | low |
| confidence | confirmed-by-reading |
| lenses | arch |
| os_sensitivity | linux-only |
| testability | A |
| probe | — |
| evidence | ts-core/src/utils/SysInfo.ts:11 —  top-level `import { createRequire } from "node:module"` is unconditional; createRequire usage inside getRequire() is guarded by typeof/import.meta checks but the module-level import itself executes on every runtime including Cloudflare Workers where node:module ... |
| fix_sketch | Convert the top-level static import to a dynamic import inside the guarded branch: replace `import { createRequire } from "node:module"` with a lazy `const { createRequire } = await import("node:mo... |

#### Manifestations / affected surfaces

| id | zone | severity | confidence | lenses | os_sensitivity | testability | evidence (snippet) |
|----|------|----------|------------|--------|----------------|-------------|-------------------|
| `phase0-ts-core-node-module-utils-index-01` | phase0 | low | confirmed-by-reading | arch | linux-only | A | ts-core/src/utils/index.ts:10 —  top-level `import { createRequire } from "node:module"` is unconditional; executed at module load time on all runtimes including edge (Cloudflare Workers) where nod... |
| `phase0-ts-core-node-module-core-index-01` | phase0 | low | confirmed-by-reading | arch | linux-only | A | ts-core/src/core/index.ts:8 —  top-level `import { createRequire } from "node:module"` is unconditional; executed at module load on all runtimes including Cloudflare Workers. The getRequire() helpe... |
| `phase0-ts-core-node-crypto-ConfigUtils-01` | phase0 | low | confirmed-by-reading | arch | linux-only | A | ts-core/src/configs/ConfigUtils.ts:6 —  top-level `import crypto from "node:crypto"` is unconditional; the only function in the file (decryptConfig) uses crypto.createDecipheriv directly with no ru... |
| `facade-node-module-crypto-edge-bundle-01` | facade | low | confirmed-by-probe | edge,arch | linux-only | A | ts-cloud/src/platform/cloudflare/worker.ts:1 imports { logger } from @ckir/corelib, which via ts-core/src/index.ts re-exports ts-core/src/core/index.ts (phase0-ts-core-node-module-core-index-01), t... |

---

### Cluster: `redb-concurrent-persist-load-robust`

**Max severity:** low · **Best confidence:** confirmed-by-probe · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `engine-redb-concurrent-persist-load-robust-01` |
| zone | engine |
| severity | low |
| confidence | confirmed-by-probe |
| lenses | races |
| os_sensitivity | cross-os (Linux leg pending-ci) |
| testability | A |
| probe | `probes/rust/tests/redb_concurrent.rs::q2_concurrent_persist_and_load_is_consistent` |
| evidence | POSITIVE RESULT (no defect). The same-host redb persist/load path is robust under concurrency. host.rs:132 subscribe (begin_write/open_table/insert/commit) and host.rs:72 get_persisted_subscriptions (begin_read/open_table/iter) share one Arc<Database>; redb 4.1.0 is MVCC (serialized writers, snap... |
| fix_sketch | None needed (positive result). Probe retained as a regression guard against future changes to the subscribe/get_persisted_subscriptions redb path. |

---

### Cluster: `ffi-poisoned-config-boundary-robust`

**Max severity:** low · **Best confidence:** confirmed-by-reading · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `ffi-poisoned-config-panic-01` |
| zone | ffi |
| severity | low |
| confidence | confirmed-by-reading |
| lenses | edge |
| os_sensitivity | cross-os |
| testability | A |
| probe | — |
| evidence | Hypothesis: boot-layer config races (boot-ConfigManager-initialize-races-01/-partial-init-window-02/-loadExternalConfig-concurrent-05, affected:[ffi]) could push undefined/malformed/empty config across the JS->Rust N-API boundary; if Rust .unwrap()s, the panic aborts the whole Node process (139/1... |
| fix_sketch | No fix required for the input vector —  the napi boundary already ?-propagates type-marshalling failures as napi::Error to JS. Defense-in-depth / hardening only: (1) replace host.rs:57 Database::cr... |

---

### Cluster: `detectRuntime-uncached`

**Max severity:** low · **Best confidence:** confirmed-by-reading · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `boot-detectRuntime-uncached-06` |
| zone | boot |
| severity | low |
| confidence | confirmed-by-reading |
| lenses | perf |
| os_sensitivity | cross-os |
| testability | C |
| probe | — |
| evidence | ts-core/src/utils/runtime.ts:28-111 —  detectRuntime() recomputes the full probe ladder (RUNTIME env read, multiple globalThis property accesses, several process.env reads, try/catch blocks) on EVERY call; the result is never memoized. The runtime cannot change during a process lifetime, so this ... |
| fix_sketch | Memoize: compute once into a module-level `let cached: Runtime \| undefined` and return it on subsequent calls (export a resetForTests() if test isolation needs to re-evaluate). Note loggers/index.t... |

---

### Cluster: `gcp-logger-stray-console-calls`

**Max severity:** low · **Best confidence:** confirmed-by-reading · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `phase0-logger-gcp-console-01` |
| zone | phase0 |
| severity | low |
| confidence | confirmed-by-reading |
| lenses | arch |
| os_sensitivity | cross-os |
| testability | A |
| probe | — |
| evidence | ts-core/src/loggers/implementations/gcp.ts:8,31,33,36 —  four stray console.log/console.error calls inside createGcpLogger() bypass the structured StrictLogger interface; they emit unstructured plaintext to stdout/stderr during logger initialization rather than routing through the structured logg... |
| fix_sketch | Remove the four console.* calls from createGcpLogger(). Initialization progress does not need to be logged in production; if needed, route through a temporary bootstrap pino instance or suppress en... |

---

### Cluster: `finnhub-no-endpoint-override`

**Max severity:** low · **Best confidence:** confirmed-by-reading · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `engine-finnhub-no-endpoint-override-01` |
| zone | engine |
| severity | low |
| confidence | confirmed-by-reading |
| lenses | arch, edge |
| os_sensitivity | cross-os |
| testability | D |
| probe | — |
| evidence | rust/src/markets/nasdaq/datafeeds/streaming/finnhub/finnhub_driver.rs:125 — driver hardcodes `format!("{FINNHUB_WS}?token={}", self.token)` (FINNHUB_WS const at :101); FinnhubConfig (finnhub_streamer.rs:133) exposes only `token`/`name`, no `base_url` override. Unlike yahoo (yahoo_driver.rs:145/18... |
| fix_sketch | Add an optional `base_url: Option<String>` to FinnhubConfig and have the driver resolve `self.base_url.clone().unwrap_or_else(\|\| FINNHUB_WS.to_string())` before appending `?token=` (mirroring yahoo... |

---

### Cluster: `engine-reconnect-teardown-loom`

**Max severity:** low · **Best confidence:** confirmed-by-reading · **Members:** 1

#### Owner finding

| Field | Value |
|-------|-------|
| **id** | `engine-reconnect-teardown-loom-01` |
| zone | engine |
| severity | low |
| confidence | confirmed-by-reading |
| lenses | races |
| os_sensitivity | cross-os |
| testability | B |
| probe | — |
| evidence | Standalone loom model of the streaming-engine reconnect/teardown ordering explored ALL interleavings of a 2-thread race (supervisor/driver emit+reset vs Drop teardown) with NO deadlock, NO send-after-close panic, NO lost-shutdown, and consistent backoff. Loom confirmed engaged (>1 interleaving): ... |
| fix_sketch | None required - exploration clean. The durable loom probe (probes/rust/tests/reconnect_teardown_loom.rs, run via RUSTFLAGS="--cfg loom" cargo test --features loom) guards the modeled ordering again... |

---

## Appendix — All Findings (ranked)

Ranked by severity then confidence. Each finding appears exactly once.

| # | id | zone | severity | confidence | lenses | os | testability | cluster | probe | evidence (snippet) | fix_sketch (snippet) |
|---|-----|------|----------|------------|--------|----|-------------|---------|-------|-------------------|----------------------|
| 1 | `boot-ConfigManager-cli-override-process-exit-07` | boot | high | confirmed-by-probe | edge,arch | cross-os | A | `configmanager-cli-argv-hazards` | `probes/js/configmanager-init-race.probe.test.ts` | ts-core/src/configs/ConfigManager.ts:168-172 —  initialize() builds a commander Command with allowUnknownOption(true) (:170) intending unknown --flags to flow to applyCliOverrid... | Stop commander from exiting the process inside a library: call program.exitOverride() (throw instead of exit) and cat... |
| 2 | `boot-ConfigManager-initialize-races-01` | boot | high | confirmed-by-reading | races,arch | cross-os | B | `configmanager-concurrency-races` | — | ts-core/src/configs/ConfigManager.ts:160-190 —  async initialize() has NO double-init/concurrent-init guard. It is async with multiple await points (parseAsync :172, fetchExtern... | Add an init guard: cache a private _initPromise on first initialize() call and return it for subsequent concurrent ca... |
| 3 | `boot-ConfigManager-partial-init-window-02` | boot | high | confirmed-by-reading | races,edge | cross-os | B | `configmanager-concurrency-races` | — | ts-core/src/configs/ConfigManager.ts:115-120,160-190 —  getInstance() returns a usable singleton whose _config starts as {} (:87) and is only populated after the async initializ... | Expose an isInitialized()/ready Promise and have get() either await readiness or throw/warn when read before initiali... |
| 4 | `engine-redb-open-expect-abort-01` | engine | medium | confirmed-by-probe | races,edge | cross-os (Linux leg pending-ci; redb file-locking differs Win/Linux) | C | `redb-double-open-process-abort` | `probes/rust/tests/redb_concurrent.rs::q3_shared_path_double_open_panics` | rust/src/markets/nasdaq/datafeeds/streaming/core/host.rs:57 —  WebsocketStreamerHost::new does `Database::create(&db_path).expect("Failed to open redb")`. A failed redb open (pa... | Replace .expect() with `?`-propagation: have WebsocketStreamerHost::new return napi::Result<Self> (or a Result the na... |
| 5 | `phase0-logger-raw-error-sqlite-01` | phase0 | medium | confirmed-by-reading | arch | cross-os | A | `error-serialization-log-gaps` | — | ts-core/src/database/sqlite/sqlite-db.ts:49,114 —  raw caught error `e` passed directly as `error: e` into logger extras without serializeError(). Non-Error objects or Error sub... | Wrap both occurrences with serializeError: `error: serializeError(e)`. serializeError is already used in ts-core (see... |
| 6 | `phase0-logger-raw-error-postgres-01` | phase0 | medium | confirmed-by-reading | arch | cross-os | A | `error-serialization-log-gaps` | — | ts-core/src/database/postgres/postgres-db.ts:50,115 —  raw caught error `e` passed directly as `error: e` into logger extras without serializeError(). Same issue as sqlite-db: n... | Wrap both occurrences with serializeError: `error: serializeError(e)`. serializeError is already a dep used in ts-cor... |
| 7 | `phase0-logger-raw-error-router-01` | phase0 | medium | confirmed-by-reading | arch | cross-os | A | `error-serialization-log-gaps` | — | ts-cloud/src/core/router.ts:137 —  `error: err.message` in onError handler only captures message string, discarding stack trace and cause chain. In production this leaves unhand... | Use `error: serializeError(err)` (import serializeError from serialize-error) to capture the full error including sta... |
| 8 | `phase0-logger-raw-error-Top100-01` | phase0 | medium | confirmed-by-reading | arch | cross-os | A | `error-serialization-log-gaps` | — | ts-markets/src/nasdaq/groups/Top100.ts:125 —  `error: error instanceof Error ? error.message : String(error)` manually extracts message string; discards stack, cause, and non-en... | Replace with `error: serializeError(error)` (import serializeError from serialize-error). NOT implemented this cycle. |
| 9 | `boot-RequestUnlimited-retry-limit-unbounded-03` | boot | medium | confirmed-by-reading | edge,perf | cross-os | B | `http-retry-config-hazards` | — | ts-core/src/retrieve/RequestUnlimited.ts:112-116,121-130 —  cfgRetryLimit is read from ConfigManager.get(retrieve.retry.limit) with NO upper-bound validation and fed straight in... | Clamp/validate config-derived retry params: coerce to integer, enforce a sane MAX (e.g. limit <= 10, backoffLimit <= ... |
| 10 | `boot-ConfigManager-loadExternalConfig-concurrent-05` | boot | medium | confirmed-by-reading | races | cross-os | B | `configmanager-concurrency-races` | — | ts-core/src/configs/ConfigManager.ts:204-230 —  loadExternalConfig() is a public async runtime mutator with no guard against concurrent invocation or concurrency with initialize... | Serialize all config mutators (initialize, loadExternalConfig, updateValue) through a single internal async queue/mut... |
| 11 | `facade-worker-bundle-size-perf-01` | facade | medium | confirmed-by-reading | perf,edge | linux-only | C | `worker-bundle-size-and-platform` | — | ts-cloud/src/platform/cloudflare/worker.ts —  wrangler deploy --dry-run reports Total Upload: 6438.69 KiB / gzip: 901.18 KiB for a single-file worker bundle. Oversized contribut... | Change tsup cloudflare worker entry to platform:"browser" so Node built-ins are excluded and bundle size drops signif... |
| 12 | `facade-market-status-error-status-swallowed-01` | facade | medium | confirmed-by-reading | arch | cross-os | A | `market-status-http-error-swallow` | — | ts-cloud/src/markets/nasdaq/MarketStatusCloud.ts:42,54 —  the catch block constructs a fatalResult object and returns c.json(fatalResult, 200) hardcoded. This swallows the HTTP ... | Change the catch-branch return from c.json(fatalResult, 200) to c.json(fatalResult, 500) (or read the status from the... |
| 13 | `facade-worker-tsup-platform-node-01` | facade | medium | confirmed-by-reading | edge,arch | linux-only | A | `worker-bundle-size-and-platform` | — | ts-cloud/tsup.config.ts:8 —  the cloudflare worker bundle entry uses platform:"node" (esbuild node platform). This tells esbuild that Node.js built-in modules are available, so ... | Confirm whether anything deploys dist/cloudflare (if not, drop the cloudflare tsup entry entirely — wrangler bundles ... |
| 14 | `boot-ConfigManager-process-argv-unguarded-08` | boot | medium | confirmed-by-reading | edge,arch | linux-only | A | `configmanager-cli-argv-hazards` | — | ts-core/src/configs/ConfigManager.ts:165 —  initialize() does `const argv = args ?? process.argv.slice(2)`; process.argv is accessed unguarded. On edge runtimes where process is... | Guard the access: `const argv = args ?? (typeof process !== "undefined" && Array.isArray(process.argv) ? process.argv... |
| 15 | `boot-ConfigManager-sysconfig-reference-severance-09` | boot | medium | confirmed-by-reading | races,edge | cross-os | B | `configmanager-concurrency-races` | — | ts-core/src/configs/ConfigManager.ts:107-109 (constructor binds globalThis.sysconfig = this._config, the EMPTY initial object = Ref A) vs :162 (loadDefaults reassigns this._conf... | MUTATE the existing _config/globalThis.sysconfig object in place (Object.assign/clear-and-fill) instead of reassignin... |
| 16 | `ffi-reentrancy-reconnect-gc-deadlock-01` | ffi | medium | suspected | races | cross-os | A | `ffi-reentrancy-reconnect-gc` | — | HYPOTHESIS (spec §8): under rapid subscribe/unsubscribe churn + forced reconnects + JS GC, the Rust->JS threadsafe_function (on_pricing/on_market, fired NonBlocking from the alp... | No code change. To raise confidence to confirmed-suspected-clean or flip to a real finding, the probe must actually d... |
| 17 | `facade-node-module-crypto-edge-bundle-01` | facade | low | confirmed-by-probe | edge,arch | linux-only | A | `ts-core-node-imports-edge-compat` | `probes/_harness/edge-boot.mjs` | ts-cloud/src/platform/cloudflare/worker.ts:1 imports { logger } from @ckir/corelib, which via ts-core/src/index.ts re-exports ts-core/src/core/index.ts (phase0-ts-core-node-modu... | No runtime break to fix; this is a fragility/conformance nit. To make ts-core natively edge-clean (independent of nod... |
| 18 | `engine-redb-concurrent-persist-load-robust-01` | engine | low | confirmed-by-probe | races | cross-os (Linux leg pending-ci) | A | `redb-concurrent-persist-load-robust` | `probes/rust/tests/redb_concurrent.rs::q2_concurrent_persist_and_load_is_consistent` | POSITIVE RESULT (no defect). The same-host redb persist/load path is robust under concurrency. host.rs:132 subscribe (begin_write/open_table/insert/commit) and host.rs:72 get_pe... | None needed (positive result). Probe retained as a regression guard against future changes to the subscribe/get_persi... |
| 19 | `engine-finnhub-no-endpoint-override-01` | engine | low | confirmed-by-reading | arch,edge | cross-os | D | `finnhub-no-endpoint-override` | — | rust/src/markets/nasdaq/datafeeds/streaming/finnhub/finnhub_driver.rs:125 — driver hardcodes `format!("{FINNHUB_WS}?token={}", self.token)` (FINNHUB_WS const at :101); FinnhubCo... | Add an optional `base_url: Option<String>` to FinnhubConfig and have the driver resolve `self.base_url.clone().unwrap... |
| 20 | `phase0-ts-core-node-module-SysInfo-01` | phase0 | low | confirmed-by-reading | arch | linux-only | A | `ts-core-node-imports-edge-compat` | — | ts-core/src/utils/SysInfo.ts:11 —  top-level `import { createRequire } from "node:module"` is unconditional; createRequire usage inside getRequire() is guarded by typeof/import.... | Convert the top-level static import to a dynamic import inside the guarded branch: replace `import { createRequire } ... |
| 21 | `phase0-ts-core-node-module-utils-index-01` | phase0 | low | confirmed-by-reading | arch | linux-only | A | `ts-core-node-imports-edge-compat` | — | ts-core/src/utils/index.ts:10 —  top-level `import { createRequire } from "node:module"` is unconditional; executed at module load time on all runtimes including edge (Cloudflar... | Same as SysInfo.ts: convert the static top-level import to a lazy dynamic import placed inside the guarded branch in ... |
| 22 | `phase0-ts-core-node-module-core-index-01` | phase0 | low | confirmed-by-reading | arch | linux-only | A | `ts-core-node-imports-edge-compat` | — | ts-core/src/core/index.ts:8 —  top-level `import { createRequire } from "node:module"` is unconditional; executed at module load on all runtimes including Cloudflare Workers. Th... | Convert the static import to a lazy dynamic import inside the isNodeLike branch in getRequire(). Since this file alre... |
| 23 | `phase0-ts-core-node-crypto-ConfigUtils-01` | phase0 | low | confirmed-by-reading | arch | linux-only | A | `ts-core-node-imports-edge-compat` | — | ts-core/src/configs/ConfigUtils.ts:6 —  top-level `import crypto from "node:crypto"` is unconditional; the only function in the file (decryptConfig) uses crypto.createDecipheriv... | Replace the static node:crypto import with the Web Crypto API (globalThis.crypto / SubtleCrypto), which is available ... |
| 24 | `phase0-logger-gcp-console-01` | phase0 | low | confirmed-by-reading | arch | cross-os | A | `gcp-logger-stray-console-calls` | — | ts-core/src/loggers/implementations/gcp.ts:8,31,33,36 —  four stray console.log/console.error calls inside createGcpLogger() bypass the structured StrictLogger interface; they e... | Remove the four console.* calls from createGcpLogger(). Initialization progress does not need to be logged in product... |
| 25 | `boot-RequestUnlimited-backoff-no-jitter-04` | boot | low | confirmed-by-reading | perf,races | cross-os | C | `http-retry-config-hazards` | — | ts-core/src/retrieve/RequestUnlimited.ts:41-54 —  retry config sets limit/backoffLimit but provides no jitter; ky default backoff is deterministic exponential (0.3*2^n capped at... | Add randomized jitter to the retry schedule —  either supply ky a custom delay function `(n) => baseBackoff(n) * (0.5... |
| 26 | `boot-detectRuntime-uncached-06` | boot | low | confirmed-by-reading | perf | cross-os | C | `detectRuntime-uncached` | — | ts-core/src/utils/runtime.ts:28-111 —  detectRuntime() recomputes the full probe ladder (RUNTIME env read, multiple globalThis property accesses, several process.env reads, try/... | Memoize: compute once into a module-level `let cached: Runtime \| undefined` and return it on subsequent calls (export... |
| 27 | `ffi-poisoned-config-panic-01` | ffi | low | confirmed-by-reading | edge | cross-os | A | `ffi-poisoned-config-boundary-robust` | — | Hypothesis: boot-layer config races (boot-ConfigManager-initialize-races-01/-partial-init-window-02/-loadExternalConfig-concurrent-05, affected:[ffi]) could push undefined/malfo... | No fix required for the input vector —  the napi boundary already ?-propagates type-marshalling failures as napi::Err... |
| 28 | `engine-reconnect-teardown-loom-01` | engine | low | confirmed-by-reading | races | cross-os | B | `engine-reconnect-teardown-loom` | — | Standalone loom model of the streaming-engine reconnect/teardown ordering explored ALL interleavings of a 2-thread race (supervisor/driver emit+reset vs Drop teardown) with NO d... | None required - exploration clean. The durable loom probe (probes/rust/tests/reconnect_teardown_loom.rs, run via RUST... |

_End of report. 28 findings · 15 clusters · generated 2026-06-13_
