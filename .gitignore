# Nx + pnpm + Rust + TS ignores
node_modules
dist
.nx
rust/target
*.log
.env.local
corelib.txt
lint-output.txt
*.node
*.tgz
.pnpm-store

# token-discipline + Antigravity dev-workflow (local working artifacts)
.claude/token-ledger.ndjson
CLAUDE-TO-ANTIGRAVITY.md
.agent/
ts-core/.agent/
.serena/

# ──────────────────────────────────────────────
# AWS SAM deployment folder
# Keep ONLY template.yaml, ignore everything else
# ──────────────────────────────────────────────
sam-corelib/*
!sam-corelib/template.yaml
!sam-corelib/samconfig.toml
!sam-corelib/event.json
!sam-corelib/events

# SAM build output (always ignore)
sam-corelib/.aws-sam/
sam-corelib/*.js
sam-corelib/*.zip
sam-corelib/*.tgz

full-modules.svg
/rust/docs
/ts-cloud/docs
/ts-core/docs
/ts-markets/docs
__pycache__

# /probes build artifacts (the probe SOURCES are durable + tracked)
probes/**/target
probes/**/node_modules
